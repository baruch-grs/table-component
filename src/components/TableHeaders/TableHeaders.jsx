import React from "react";

const TableHeaders = ({ headers }) => {
  console.log(headers);
  return (
    <>
      <thead>
        <tr>
          {/* {tableHeaders.map((header) => (
            <th>{header}</th>
          ))} */}
        </tr>
      </thead>
    </>
  );
};

export default TableHeaders;
